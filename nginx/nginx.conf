server {
    listen 80;
    server_name localhost;

    # Docker's embedded DNS — re-resolve upstream hostnames every 5 s.
    # The variable-based proxy_pass below is required for this to take effect.
    resolver 127.0.0.11 valid=5s ipv6=off;
    resolver_timeout 3s;

    # Order Listener webhook & API
    location /api/listener/ {
        set $upstream http://order-listener:8001;
        rewrite ^/api/listener/(.*)$ /$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Webhook-Signature $http_x_webhook_signature;
    }

    # Order Generator API
    location /api/generator/ {
        set $upstream http://order-generator:8002;
        rewrite ^/api/generator/(.*)$ /$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # AI models API (proxied through dashboard-api)
    location /api/ai/ {
        set $upstream http://dashboard-api:8003;
        rewrite ^/api/ai/(.*)$ /ai/$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Notification Service API (VAPID key, push subscriptions)
    location /api/notifications/ {
        set $upstream http://notification-service:8010;
        rewrite ^/api/notifications/(.*)$ /$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Strategy Tester API
    location /api/tester/ {
        set $upstream http://strategy-tester:8006;
        rewrite ^/api/tester/(.*)$ /$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Tester UI (React SPA)
    location /tester/ {
        set $upstream http://tester-ui:3001;
        rewrite ^/tester/(.*)$ /$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Dashboard REST API
    location /api/dashboard/ {
        set $upstream http://dashboard-api:8003;
        rewrite ^/api/dashboard/(.*)$ /$1 break;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # WebSocket for live order feed
    location /ws/ {
        set $upstream http://dashboard-api:8003;
        proxy_pass $upstream;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_read_timeout 86400;
    }

    # Dashboard UI (React SPA) - Assets
    location /assets/ {
        set $upstream http://dashboard-ui:3000;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Dashboard UI (React SPA) - Favicon / manifest
    location = /favicon.svg {
        set $upstream http://dashboard-ui:3000;
        proxy_pass $upstream;
        proxy_set_header Host $host;
    }

    # Dashboard UI (React SPA) - Index
    location / {
        set $upstream http://dashboard-ui:3000;
        proxy_pass $upstream;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
